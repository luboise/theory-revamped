{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Connect to local",
			"type": "firefox",
			"request": "launch",
			"reAttach": true,
			"url": "http://localhost:3000",

			"enableCRAWorkaround": true,
			"webRoot": "${workspaceFolder}"
		}
	]
}
